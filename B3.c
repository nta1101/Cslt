#include <stdio.h>

int main()
{
    //int n;
    //float m;
    int A[10];
    //printf("so phan tu n: ");
    //scanf("%d", n);

    //printf("nhap vao mot so thuc: ");
    //scanf("%f", &m);

    for(int i = 0; i < 5; i++)
    {
        printf("A[] = ");
        scanf(A[i]);
    }
    return 0;
}